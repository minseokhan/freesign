# FreeSign — 화면별 조작 가이드 (브라우저 워크스루)

`npm run dev` → `http://localhost:3000` 으로 접속했을 때, **어느 메뉴에서 무엇이 보이고 / 어떤 버튼이 있고 / 무엇을 입력하며 / 눌렀을 때 무슨 일이 일어나는지**를 화면 단위로 정리한 문서입니다. 실제 코드 기준이며, 브라우저에서 직접 클릭하며 확인하거나 수정·테스트할 때 체크리스트로 쓰세요.

> **읽는 법**: 각 화면마다 `📍 보이는 것` / `⌨️ 입력` / `🔘 버튼·동작` / `✅ 확인 포인트` 로 구분했습니다.

---

## 0. 공통 규칙 (모든 화면에 적용)

- **로그인 가드**: `/dashboard` 이하 모든 화면은 로그인 필수. 미로그인 상태로 접근하면 렌더 전에 차단되고 `/login`으로 보내집니다.
- **진입점 `/`**: UI 없이 즉시 리다이렉트. 로그인 상태면 `/dashboard`, 아니면 `/login`.
- **공통 레이아웃**: 왼쪽(모바일은 상단)에 사이드바, 상단 헤더에 작은 `FreeSign` 라벨 + 큰 제목 "정산 워크스페이스". 헤더 오른쪽 점선 박스는 장식(클릭 동작 없음).
- **금액 표기**: 전부 원화 `₩1,234,000` 형식. 세 자리 콤마.
- **날짜 표기**: 목록·상세는 `YYYY.MM.DD`, 입력 폼은 `YYYY-MM-DD` 날짜 선택기.

### 사이드바 메뉴 (위 → 아래)
| 라벨 | 이동 경로 |
|------|-----------|
| FreeSign(로고) | `/dashboard` |
| 대시보드 | `/dashboard` |
| 클라이언트 | `/clients` |
| 계약 | `/contracts` |
| 인보이스 | `/invoices` |
| 리포트 | `/reports` |
| 설정 | `/settings` |

현재 보고 있는 메뉴는 파란색으로 강조됩니다.

---

## 1. `/login` — 로그인

**📍 보이는 것**: 중앙 카드, `FreeSign` 라벨, 제목 "로그인", 안내문 "Google 계정으로 계약과 정산 기록을 안전하게 관리하세요."

**🔘 버튼·동작**
- **"Google로 계속하기"** (파란 버튼, 유일한 조작) → 구글 OAuth 시작. `NEXT_PUBLIC_SITE_URL` + `/auth/callback`으로 리다이렉트 구성 후 구글 로그인 페이지로 이동.
- 로그인 실패 시 `/login?error=oauth`로 돌아오며 빨간 배너: "Google 로그인을 시작하지 못했습니다. 잠시 후 다시 시도해 주세요."

**✅ 확인 포인트**
- 버튼을 눌렀을 때 구글 계정 선택 화면으로 넘어가는가.
- 로그인 후 `/dashboard`로 돌아오는가. (돌아오지 못하고 에러 배너가 뜨면 → Supabase Redirect URL / `NEXT_PUBLIC_SITE_URL` 설정 확인)
- 입력 필드는 없음(구글 단일 버튼 방식).

---

## 2. `/dashboard` — 대시보드 (핵심 화면)

데이터 유무에 따라 두 모습으로 나뉩니다.

### 2-A. 빈 상태 (데이터 0건 — 첫 로그인 직후 예상 모습)
**📍 보이는 것**: "FS" 로고, 제목 "아직 대시보드 데이터가 없어요", 안내문, 그리고 **데모 데이터 채우기 버튼** 하나.

> ⚠️ **텅 빈 대시보드는 고장이 아니라 정상입니다.** 아래 버튼으로 샘플을 채우세요.

### 2-B. 데이터 있는 상태
**📍 보이는 것**
- 제목 "대시보드" + 우측 **데모 데이터 버튼**.
- **KPI 카드 2개**:
  - **미수금 합계** (빨강) — 미입금 인보이스 청구 총액.
  - **이달 수익** (초록) — KST 입금월 기준 실지급액 합계.
- **위젯: 임박/지연 지급기한** — 미입금 인보이스 중 지연(빨강 "지연" 배지) 또는 7일 이내 임박(앰버 "임박" 배지)만 표시. 표: 클라이언트 / 계약(클릭 시 해당 인보이스로) / 금액 / 지급기한 / 상태. 없으면 "지연되었거나 곧 지급기한이 도래하는 인보이스가 없습니다."
  - **채널 수익 TOP** — 입금 완료 인보이스 기준 상위 5개 채널 순위. 없으면 "아직 입금 완료된 인보이스가 없습니다."

### 🔘 데모 데이터 버튼 (대시보드 우상단 / 빈 상태 중앙)
- **데이터가 없을 때** → 파란 **"데모 데이터 채우기"**. 클릭 시 확인창 없이 바로 샘플 생성("무디" 클라이언트 + "무디 브랜드 리뉴얼" 계약(서명완료·300만원) + 입금완료 인보이스 1건). 진행 중 "채우는 중" → 성공 시 "데모 데이터를 채웠습니다." + 새로고침.
- **데이터가 있을 때** → 빨간 **"데모 데이터 지우기"**. 클릭 시 확인창 "데모 데이터를 지울까요? 실제 계약과 인보이스는 삭제되지 않습니다." → 확인 시 데모(`is_demo`)만 삭제. **실데이터는 안 지워짐.**

**✅ 확인 포인트**
- "데모 데이터 채우기" 후 KPI·위젯에 숫자가 채워지는가.
- 나중에 직접 만든 인보이스를 정산(paid)하면 "이달 수익"에 반영되는가.
- "데모 데이터 지우기"가 내가 직접 만든 데이터는 건드리지 않는가.

---

## 3. `/clients` — 클라이언트

**📍 보이는 것**: 제목 "클라이언트" + 우상단 **"클라이언트 만들기"**(→ `/clients/new`). 아래 **채널 필터 탭**: 전체 / 링크드인 / 인스타그램 / 유튜브 / 직거래 / 크몽 / 추천 / 기타 (클릭 시 `?channel=` 필터).
- 목록 표: 이름(클릭 시 상세) / 채널 배지 / 연락처(이메일·전화) / "보기" 링크.
- 빈 상태: "아직 클라이언트가 없어요" + "클라이언트 만들기" 버튼.

**✅ 확인 포인트**: 채널 탭을 누르면 해당 채널만 필터되는가. 이름 클릭 시 상세로 가는가.

### 3-1. `/clients/new` — 새 클라이언트
**⌨️ 입력** (폼)
| 필드 | 타입 | 필수 | 규칙 |
|------|------|------|------|
| 이름 | 텍스트 | ✅ | 공백 제외 1자 이상 |
| 채널 | 드롭다운 | ✅ | 링크드인/인스타그램/유튜브/직거래(기본)/크몽/추천/기타 |
| 이메일 | email | – | 입력 시 이메일 형식 검증 |
| 전화번호 | tel | – | 형식 제약 없음 |
| 메모 | textarea | – | 자유 입력 |

**🔘 버튼**: **취소**(이전 화면) / **저장**. 저장 성공 시 → `/clients` 목록으로 이동, "저장했습니다." 표시. 검증 실패 시 각 필드 아래 빨간 오류.

**✅ 확인 포인트**: 이름 비우고 저장 → 오류 뜨는가. 잘못된 이메일 형식 → 오류 뜨는가. 저장 후 목록에 나타나는가.

### 3-2. `/clients/[id]` — 클라이언트 상세
**📍 보이는 것**: 이름 + 채널 배지, "기본 정보" 카드(이메일/전화/메모, 없으면 "등록되지 않음").
**🔘 버튼**: **수정**(→ edit) / **삭제**(빨강).
- 삭제 클릭 시 확인창 "이 클라이언트를 삭제할까요? 계약과 인보이스 기록은 보존됩니다." → 확인 시 `/clients`로 이동.

### 3-3. `/clients/[id]/edit` — 클라이언트 수정
- 3-1과 같은 폼(값이 채워진 상태). 저장 시 → 상세로 이동.

---

## 4. `/contracts` — 계약

**📍 보이는 것**: 제목 "계약" + 우상단 **"계약 만들기"**. **상태 필터 탭**: 전체 / 초안 / 서명완료 / 진행중 / 완료 / 취소.
- 목록 표: 제목(클릭 시 상세) / 클라이언트 / 금액 / 상태 배지 / 기간 / "보기".
- 빈 상태: "아직 계약이 없어요" + "계약 만들기".

### 계약 상태(status) 5종 & 배지
| 내부값 | 배지 라벨 | 색 |
|--------|-----------|-----|
| draft | 초안 | 회색 |
| signed | 서명완료 | 앰버 |
| active | 진행중 | 앰버 |
| done | 완료 | 초록 |
| canceled | 취소 | 회색 |

**상태 전이 규칙**: draft→(signed/canceled) · signed→(active/draft/canceled) · active→(done/draft/canceled) · done/canceled=종료.
※ draft로 되돌리면 서명 데이터(서명 이미지·문서 해시)가 초기화됩니다.

### 4-1. `/contracts/new` — 계약 만들기 (AI 초안)
> 클라이언트가 0명이면 "클라이언트가 필요합니다" 카드가 뜨고 폼이 비활성. 먼저 클라이언트를 만드세요.

**스텝 표시**: ① 구조화 입력 → ② 초안 생성 → ③ 저장

**⌨️ 입력** (1단계)
| 필드 | 타입 | 필수 | 규칙 |
|------|------|------|------|
| 클라이언트 | 드롭다운 | ✅ | 등록된 클라이언트 중 선택 |
| 업무 범위 | textarea | ✅ | 1자 이상 |
| 계약 금액 | number | ✅ | 양의 정수 |
| 시작일 | date | ✅ | |
| 종료일 | date | ✅ | 시작일 이후여야 함 |
| 지급기한 | date | – | 선택 |

**🔘 흐름**
1. **"초안 생성"** 클릭 → `POST /api/contracts/draft` 호출 → 아래에 **초안 미리보기 카드** 등장(페이지 이동 없음).
   - `ANTHROPIC_API_KEY`가 있으면 "AI 보강 초안", 없으면 "골격 초안"으로 생성(기능은 안 막힘).
   - **AI 면책 배너**(앰버): "AI 초안이며 법적 자문이 아닙니다. 계약 확정 전 전문가 검토를 권장합니다."
   - 평문요약 + 조항 앞 3개 미리보기("검토 필요" 배지 포함).
2. **"다시 입력"**(입력 단계로 복귀) 또는 **"초안 저장"** 선택.
3. "초안 저장" → `draft` 계약 생성 후 `/contracts/[id]` 상세로 이동.

**✅ 확인 포인트**: 종료일을 시작일보다 앞으로 → 오류 뜨는가. 초안 생성 시 미리보기가 뜨는가. 저장 후 상세로 이동하며 목록에 "초안" 상태로 나타나는가.

### 4-2. `/contracts/[id]` — 계약 상세
**📍 보이는 것 (카드 순서대로)**
- **헤더**: 제목 + 상태 배지, 클라이언트명. 우측: (draft일 때만) **조항 편집** 버튼 / **PDF** 버튼(새 탭으로 `/api/contracts/[id]/pdf`).
- **기본 정보**: 금액 / 기간 / 문서 해시 / PDF 저장 여부.
- **AI 면책 배너**(앰버).
- **업무 범위**: scope 원문.
- **조항과 평문요약**: 조항별 본문 + 요약("검토 필요" 배지).
- **서명 카드** ↓
- **다음 단계**(상태 전이 버튼) ↓
- **이력 타임라인**: 상태 전이·서명 기록(append-only, 오래된 순).

**🔘 서명 (draft 상태에서만 캔버스 노출)**
- 캔버스(720×240)에 마우스/터치로 서명 그림.
- **"서명 완료"** → PNG로 변환 후 `POST /api/contracts/[id]/sign`. 성공 시 `draft → signed`로 바뀌고 서명 이미지·서명자·서명 시각 표시(이후 조항 읽기 전용). **"지우기"**로 캔버스 초기화.
- 서명이 비어 있으면 "서명을 먼저 입력해 주세요." 오류.

**🔘 상태 전이 버튼 (상태별로 다르게 보임)**
| 현재 상태 | 보이는 버튼 |
|-----------|-------------|
| draft | 계약 취소(빨강). ※서명은 서명 카드에서 |
| signed | 진행 시작 / 초안으로 되돌리기(빨강) / 계약 취소(빨강) |
| active | 완료 처리 / 초안으로 되돌리기(빨강) / 계약 취소(빨강) |
| done, canceled | (전이 버튼 없음) |
- 클릭 시 서버에서 상태 변경 + 타임라인 기록 → "계약 상태를 변경했습니다." 표시.
- 항상 표시되는 **"인보이스 발행"** 링크 → `/invoices/new?contract=[id]`.

**✅ 확인 포인트**: draft에서 서명 → 상태가 "서명완료"로 바뀌고 서명 이미지가 저장되는가. "진행 시작"·"완료 처리"로 상태가 넘어가는가. "초안으로 되돌리기" 시 서명이 사라지는가. 모든 변경이 타임라인에 쌓이는가. PDF 버튼이 새 탭에서 열리는가.

### 4-3. `/contracts/[id]/edit` — 조항 편집
- **draft 상태에서만** 편집 가능(서명 후엔 "조항을 편집할 수 없습니다" 안내).
- 각 조항: **조항 본문**(textarea) / **평문요약**(textarea) / **검토 필요로 표시**(체크박스) 편집. 조항 제목은 편집 불가.
- **취소** / **조항 저장** → 저장 시 상세로 이동.

---

## 5. `/invoices` — 인보이스

**📍 보이는 것**: 제목 "인보이스" + 우상단 **"계약에서 발행"**(→ `/contracts`). **상태 필터 탭**: 전체 / 초안 / 미수 / 입금완료.
- 목록 표: 클라이언트 / 계약(클릭 시 상세) / 금액 / 상태 배지 / 발행일 / 지급기한(연체 시 빨강) / "보기".
- 빈 상태: "아직 인보이스가 없어요" + "계약 목록 보기"(인보이스는 계약에서만 발행).

### 정산 상태 배지
| 상태 | 라벨 | 색 |
|------|------|-----|
| draft | 초안 | 회색 |
| unpaid | 미수 | 앰버 |
| paid | 입금완료 | 초록 |
| (unpaid + 기한 지남) | 지연 | 빨강 |

### 5-1. `/invoices/new?contract=[계약id]` — 인보이스 발행
> **반드시 계약에서 진입**해야 합니다(계약 상세의 "인보이스 발행" 링크). `?contract=` 없이 들어가면 404. 취소된 계약이면 발행 버튼 비활성.

**⌨️ 입력**
| 필드 | 타입 | 기본값 | 규칙 |
|------|------|--------|------|
| 청구 금액 | number | 계약 금액 | 양의 정수 |
| 발행일 | date | 오늘 | |
| 지급기한 | date | 오늘+30일 | 발행일 이후여야 함 |
| 원천징수 | 드롭다운 | 3.3% | 3.3% / 8.8% / 없음 |

**📍 실시간 미리보기**: 금액·원천징수 유형에 따라 **청구 금액 / 원천징수 / 실수령액** 3열이 즉시 계산되어 표시(참고용). 안내: "저장되는 금액은 서버에서 재계산되어 발행 시점으로 고정됩니다."

**원천징수 계산식** (`lib/tax.ts`):
- 소득세 = 금액 × 세율(3.3%→3% / 8.8%→8% / 없음→0%)
- 지방소득세 = 소득세의 10%
- 원천징수액 = 소득세 + 지방소득세 → 실효 세율 3.3% / 8.8%
- 실수령액 = 금액 − 원천징수액

**🔘 버튼**: **취소** / **발행**. 발행 성공 시 → `/invoices/[id]` 상세로 이동. 발행 시 상태는 항상 **미수(unpaid)** 로 생성되고, 원천징수 금액은 이 시점 스냅샷으로 고정됩니다.

**✅ 확인 포인트**: 원천징수 3.3%↔8.8% 바꿀 때 실수령액 미리보기가 바뀌는가. 지급기한을 발행일 앞으로 → 오류 뜨는가. 발행 후 상세로 이동하며 "미수" 상태로 뜨는가.

### 5-2. `/invoices/[id]` — 인보이스 상세
**📍 보이는 것**
- 헤더: 계약 제목 + 정산 상태 배지, 클라이언트명. 우측: **정산 토글**, **PDF** 버튼(새 탭).
- **정산 요약**: 청구 금액 / 원천징수 / 실수령액(발행 시점 스냅샷). "원천징수 내역 보기" 펼침.
- **지급 정보**: 발행일 / 지급기한 / 입금일 / 입금 방식. 연체 시 빨간 경고.
- **입금 계좌**: 설정(profiles)에 등록된 계좌. 없으면 "설정에 등록된 입금 계좌가 없습니다."
- **이력 타임라인**: 발행·정산 변경 기록.

**🔘 정산 토글 (낙관적 UI)**
- 현재 **미수**면 **"입금완료"**(파랑) 버튼 + "입금 방식" 입력(기본 "계좌이체").
- 현재 **입금완료**면 **"미수로 되돌리기"**(빨강) 버튼.
- 클릭 시 UI가 즉시 바뀌고(낙관적) 서버 반영. 성공 "입금완료로 변경했습니다." / 실패 시 이전 상태로 롤백 + 빨간 에러.
- paid로 바꾸면 입금일(now)·입금 방식 저장, unpaid로 되돌리면 둘 다 삭제.

**✅ 확인 포인트**: "입금완료" 토글 시 즉시 배지가 바뀌는가. 대시보드 "이달 수익"에 반영되는가. 되돌리기가 동작하는가. 타임라인에 기록이 쌓이는가.

---

## 6. `/reports` — 리포트

**📍 보이는 것**
- 제목 "리포트" + 우상단 **"CSV 내보내기"**(→ `/api/reports?year=`, 클릭 시 파일 다운로드).
- **연도 필터 탭**: 최근 5년(올해~-4년).
- **연간 입금 수익** 카드(초록 큰 숫자) = 해당 연도 입금완료 인보이스의 실지급액 합계. **입금(paid_at) 기준·KST 연도 기준**(발행일 아님).
- **채널별 수익 표**: 채널 배지 / 수익, 마지막 행 합계.
- 빈 상태: "해당 연도 입금 기록이 없어요".

**✅ 확인 포인트**: 연도 탭을 바꾸면 수치가 바뀌는가. "CSV 내보내기" 클릭 시 `freesign-report-{연도}.csv` 파일이 다운로드되는가(엑셀에서 한글이 안 깨지는가 — UTF-8 BOM 적용됨).

---

## 7. `/settings` — 설정

- **준비 중 플레이스홀더.** 제목 "설정 화면 준비 중이에요", 안내문 "프로필과 기본 원천징수율 설정은 이후 단계에서 연결됩니다."
- **"설정 저장"** 버튼은 항상 **비활성**(동작 없음). 입력 필드 없음.

> ⚠️ 인보이스 상세의 "입금 계좌"는 이 설정과 연결될 예정이나, 현재 설정 화면에서 계좌를 입력하는 UI는 없습니다(향후 연결).

---

## 8. 추천 체험 순서 (전체 흐름 한 바퀴)

FreeSign의 코어는 **계약 → 지급기한 → 입금/미수 증빙** 체인입니다.

1. `/login` → 구글 로그인
2. `/dashboard` → **"데모 데이터 채우기"** (빠른 체험) — 또는 아래를 수동으로:
3. `/clients/new` → 클라이언트 생성
4. `/contracts/new` → AI 초안 생성 → 저장 → 상세에서 **서명** → 상태 전이(진행 시작/완료) → PDF 확인
5. 계약 상세의 **"인보이스 발행"** → 원천징수 확인하고 발행 → `/invoices/[id]`에서 **정산 토글(입금완료)**
6. `/dashboard` → 방금 정산이 **이달 수익**에, 미수가 **미수금**에 반영되는지 확인
7. `/reports` → 채널별 수익 확인 후 **CSV 내보내기**

---

## 9. API 라우트 (직접 방문보다 앱에서 호출됨)

| 라우트 | 역할 | 트리거 위치 |
|--------|------|-------------|
| `GET /auth/callback` | 구글 OAuth 콜백(code→세션) | 로그인 후 자동 |
| `POST /api/contracts/draft` | AI 초안 생성 | `/contracts/new` "초안 생성" |
| `POST /api/contracts/[id]/sign` | 서명 처리(업로드·해시·상태전이) | 계약 상세 "서명 완료" |
| `GET /api/contracts/[id]/pdf` | 계약 PDF | 계약 상세 "PDF" |
| `GET /api/invoices/[id]/pdf` | 인보이스 PDF | 인보이스 상세 "PDF" |
| `GET /api/reports?year=YYYY` | 채널 수익 CSV(UTF-8 BOM) | 리포트 "CSV 내보내기" |

---

## 10. 자주 막히는 지점

- **로그인 후 바로 튕김/에러** → Supabase Redirect URL에 `http://localhost:3000/auth/callback` 등록, `NEXT_PUBLIC_SITE_URL` 확인.
- **대시보드가 텅 빔** → 정상. "데모 데이터 채우기" 누르거나 직접 데이터 생성.
- **AI 초안이 밋밋함** → `ANTHROPIC_API_KEY` 미설정 시 골격 폴백(정상). 키 넣으면 AI 보강.
- **인보이스 발행 페이지 404** → 계약 상세의 "인보이스 발행"으로 들어가야 함(`?contract=` 필요).
- **PDF가 안 열림** → PDF는 Node 런타임 전용, `npm run dev`에서 확인.
- **설정에서 계좌 입력이 안 됨** → 설정 화면은 준비 중(플레이스홀더).
