node_modules/
.next/
.next-verify/
out/
next-env.d.ts
tsconfig.tsbuildinfo

# env (시크릿) — .env.example 만 커밋
.env
.env.local
.env*.local

# python
__pycache__/
*.pyc
.pytest_cache/

# phase execution outputs
phases/**/phase*-output.json
phases/**/step*-output.json

# playwright
playwright-report/
test-results/
e2e/.auth/
# dev 이메일 아웃박스 — 원문 서명 토큰이 담긴다
.e2e-outbox.jsonl

# Vercel
.vercel

# /review-code 실행 산출물 (심각도 게이트가 읽는 판정 파일 · 중간 findings)
review-verdict.json
findings.json

# oncall(CI 실패 자동 수정) 실행 산출물 — 수정 패치에 섞이면 안 된다
oncall-report.json
oncall-failed.log
oncall.patch
oncall-files.txt

# lighthouse-loop 실행 산출물 (생성물 — 도구만 추적)
scripts/lighthouse-loop/reports/
scripts/lighthouse-loop/journal.jsonl
